PORT=4000
HOST=0.0.0.0

# After `node scripts/deploy-local.mjs`, copy from .env.local or set:
# ORACLE_RPC_URL=http://127.0.0.1:8545
# ORACLE_CHAIN_ID=31337
# COMPLIANCE_ORACLE_ADDRESS=0x...
# KEEPER_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
# SCORE_SOURCE=onchain
#
# KEEPER_PRIVATE_KEY must hold AccessManager role _ORACLE_KEEPER (id 2) on the
# deployed stack — Deploy defaults Anvil #0 for that role on local Anvil.
#
# SCORE_SOURCE=memory  → beforeSwap quotes use in-memory COA only
# SCORE_SOURCE=onchain → prefer ComplianceOracle.getScore (requires rpc env)
